// Using Passport.js. Function that is going to be used as a middleware for the 
// secure web services.
var suth = function(req, res, next){
	if (!req.isAuthenticated())
		res.send(401);
	else
		next();
}

